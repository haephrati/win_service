# SG_WinService

Generic Windows service helper (`SG_WinService`) with install/uninstall command names and a demo Visual Studio project.

Open `SG_WinService.sln` in Visual Studio.

## Unit tests

`Tests/winservice.test.js` checks service name, display name, timer, and date-format macros. Run `node Tests/winservice.test.js`.
